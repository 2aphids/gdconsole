# GDConsole

To use this plugin, you must:

- instantiate the scene located at `res://addons/gdconsole/console.tscn` in your main scene,
- place your global variables in an autoloaded script named `Variables`, and
- create a new input named "console", with a key of your choice.
